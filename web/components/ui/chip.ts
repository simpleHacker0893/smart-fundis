/**
 * A read-only outlined chip: an Assessment's status, the "Showcase — not
 * verified" label. Not a button, so it has no tap target of its own.
 */
export const CHIP = "self-start rounded-full border border-line px-3 py-1 text-sm font-medium";
