# Decisions

These are house rules. Don't reopen one silently. If one looks wrong, propose a new decision that replaces it. Full reasoning lives in `docs/PRD.md` §2 (ADR-1 to ADR-17), `docs/adr/` (ADR-18 onwards), and the architecture spec.

| # | Decision | Where |
| --- | --- | --- |
| ADR-1 | Next.js App Router PWA, shadcn and Tailwind on Vercel. Convex is the system of record. | PRD §2 |
| ADR-2 | The AI service is FastAPI, LangGraph and LangChain on Brev. | PRD §2 |
| ADR-3/12 | Cosmos Reason 2 8B (with a 2B fallback) runs on vLLM on a Brev H100. | PRD §2 |
| ADR-4 | Nemotron runs through the hosted NVIDIA API via `ChatNVIDIA`. **Superseded by D-32:** Nemotron is self-hosted with vLLM on Brev. | PRD §2 |
| ADR-5 | Redis and Celery, with `QUEUE_MODE=inline` as the fallback. | PRD §2 |
| ADR-7 | YouTube and TikTok links are showcase only and are never downloaded. | PRD §2 |
| ADR-8 | Videos are stored in Convex storage. | PRD §2 |
| ADR-9 | Brev pulls jobs from Convex and has no inbound port. | PRD §2 |
| ADR-10 | Recording uses the phone's own camera (`<input capture>`), with a Liveness code. Amended by ADR-19. | PRD §2 |
| ADR-11 | A safety item marked `no` or `unclear`, or a failed liveness check, caps the Verdict at `needs_review`. Amended by ADR-19. | PRD §2 |
| ADR-13 | LangSmith traces are masked. | PRD §2 |
| ADR-14 | Eval clips are labelled blind and faults are staged. | PRD §2 |
| ADR-15 | Monorepo with `web/`, `convex/` and `ai-service/`. | PRD §2 |
| ADR-16 | Clerk with email and Google. The phone number is collected in onboarding. | PRD §2 |
| ADR-17 | `next-intl`. **Amended:** the UI is English only for now, except that consent ships in English and Kiswahili. **Amended again by D-29:** the UI ships in English and Kiswahili. | PRD §2, spec §8 |
| ADR-18 | Roles are derived in Convex, with no copy in Clerk metadata. `/dashboard` does the routing, and the admin check needs a verified email. | `docs/adr/0018-derived-roles.md` |
| ADR-19 | The Liveness code is written on paper, read by Cosmos without being told the expected code, and compared in `rules.py`. | `docs/adr/0019-paper-liveness-code.md` |
| ADR-20 | The public **Portfolio** (opt-in photos and videos, private by default, its own publish consent) is separate from Assessment video. Assessment videos stay private and never become Portfolio items. | `docs/adr/0020-public-portfolio-separate-from-assessment-video.md` |
| ADR-21 | Contact is opt-in ("Show my phone to clients", off by default). The number is returned only by the rate-limited, logged `contact.reveal` mutation, never by a query or in HTML. Amends spec §7 and PRD US-5.7. | `docs/adr/0021-opt-in-contact-reveal.md` |
| ADR-22 | `listings` is a derived projection: one row per Listed Fundi × Trade in scope, plus an `all` row, carrying `verified`, `badgeCount`, `isExpert` and `expertTrades`. Only `syncListing` writes it. Public discovery reads only `listings`. Every Listed Fundi appears; verification is shown, not required. | `docs/adr/0022-listing-projection.md` |
| D-1 | Badges are derived from approved Assessments, with no tiers. The public profile shows nothing until an Expert approves. | spec §5 |
| D-2 | Clients have no account in the MVP. `/fundis` lists Verified Fundis only. **Amended by D-18 and D-24:** still no Client account, but `/fundis` now lists **all Listed Fundis**; verification is shown, not required. Clients can filter, reveal an opted-in phone and report without an account. **Superseded for V2 by D-30:** Clients have accounts. | spec §2 |
| D-3 | A reshoot or a failure creates a new Assessment with a new code. After two failed attempts the Assessment is `failed`. | spec §5 |
| D-4 | Appeals are allowed once, against `rejected` only, with a reason. A different Expert decides, or an Admin if there isn't one. | spec §5 |
| D-5 | The Data Co-op is a Roadmap item and is kept apart from consent. `coopInterest` means "contact me" only. | spec §7 |
| D-6 | Build in vertical slices V0–V5, with a stub worker in V1. | spec §9 |
| D-7 | GitHub Issues is the tracker. One PR per ticket, reviewed by Claude in CI, and squash-merged by the Architect. Amended by D-10: review is manual for now. | spec §11 |
| D-8 | The guard accepts 10–90 s for the MVP. Test clips are the team's own recordings plus licensed Pexels footage. | spec §6, §10 |
| D-9 | The design system is v2 "Instrument": a CAUSTIC-style dark theme (graphite `#050609`, text `#f2f4f7`, amber `#ef9a57` as the only accent), with full GSAP and Three.js motion that is lazy-loaded, and static frames under reduced motion. It replaces the green `#0B5D3B` and orange `#F28C28` brand. The honesty rules are unchanged. | `design/stitch/DESIGN.md` |
| D-10 | Code review is manual for now. There's no Claude GitHub App, no `ANTHROPIC_API_KEY` secret, and no branch protection, because the repo is private on a free plan. Each PR gets the local `/code-review` (and `convex-reviewer` for `convex/`), then the Architect reviews it on GitHub before squash-merging. `claude-review.yml` is kept but skipped until the repo variable `CLAUDE_REVIEW_ENABLED` is `true`. Amends D-7. | `planning/prompts/00-setup-once.md` |
| D-11 | `web` is the only workspace package. `convex/` stays at the root as a plain Convex functions folder, with `convex` and `typescript` in the root `package.json`. A workspace package inside the functions folder would let a `convex/node_modules` get bundled (R-11). Amends the spec's "npm workspaces for `web` and `convex`". | #2 code review |
| D-12 | The package manager is **pnpm** (`packageManager: pnpm@12.4.2`), not npm: `pnpm-workspace.yaml`, one `pnpm-lock.yaml`, `pnpm -r`/`--filter` scripts, and `pnpm dlx`/`pnpm exec` instead of `npx`. Dependency install scripts stay denied in `allowBuilds` (R-13). | operator, #2 |
| D-13 | Every local secret lives in **one file, the repo-root `.env`** (git-ignored). Apps reference it rather than keeping their own: `web/next.config.ts` loads it with `@next/env` `loadEnvConfig`, and ai-service loads it by path. There's no `web/.env.local`. Deployed values are still set in Vercel, `convex env set` and on Brev. The root `.env.local` that `convex dev` writes holds only non-secret deployment pointers (`CONVEX_DEPLOYMENT`, `CONVEX_URL`) and is loaded too. Locally the Next server process therefore holds every root secret: only `NEXT_PUBLIC_*` values may ever reach the browser, and `next.config.ts` must never get an `env:` block. Story 26 of the V0 spec ("AI keys only on the Brev box") is read as: never in the repo, in `web/` or in client code; on Brev in deployment; and in the root `.env` for local runs such as the #9 smoke. Python loads only what it needs, and `configure_tracing` copies only `LANGSMITH_*` into `os.environ`. | operator |
| D-14 | LangSmith traces go to the project **`smart-fundis-agent`** (`LANGSMITH_PROJECT`, the default when it's unset). All tracing goes through `ai-service/app/tracing.py`: one client whose `anonymizer` is an allow-list mask, so it covers inputs, outputs, metadata and error text. Strings survive only under structural keys (ids, trade, verdict, status and similar), URLs are always redacted, and errors keep only the exception type. Implements ADR-13. | `ai-service/README.md` |
| D-15 | Protected pages are guarded twice. The proxy redirects signed-out visitors using a plain pathname check (`web/lib/auth-routes.ts`), and **every protected page calls `auth.protect()` itself**, which is the real guard. Clerk advises never relying on the proxy alone, and it now discourages `createRouteMatcher`. | #3 review |
| D-16 | `users.clerkId` stores Convex `identity.tokenIdentifier` (`issuer\|subject`), the key the Convex guidelines recommend, not the raw Clerk user id. Anything joining to Clerk user ids (webhooks, Backend API) must use `identity.subject` or parse it. | #4 review |
| D-17 | **Proposed (frontend, operator request; Architect to confirm).** The landing page, header and footer follow the Stitch v3 responsive screen (`design/stitch/exports/01-landing-v3-responsive`), pulled with the Stitch MCP API using `STITCH_API_KEY` from the root `.env`. The landing page ships ahead of V3's motion layer: Stitch's CSS motion (scan line, pulse, reticle) runs behind `motion-safe`, while GSAP and Three.js stay in V3. Signed-in visitors manage their profile, settings and sign-out through Clerk's `UserButton`. | operator, 2026-09-25 |
| D-18 | **Find a Fundi (V6).** Clients filter `/fundis` by Trade, county (all 47) and free-text area (a Convex search index on the Listing's area text), with no account. `/trades` becomes the Client entry. Geohash, maps and distance stay out (ADR-6). Amends spec §12 ("search" out of scope) and D-2. | operator OD-3, `docs/superpowers/specs/2026-09-26-find-a-fundi-design.md` |
| D-19 | ~~Listed requires at least one Badge; Portfolio-only Fundis are not listed.~~ **Superseded by D-24** (operator Q-1). | — |
| D-20 | The public profile may show public Portfolio items ("Not verified"), self-declared **Rates** ("Set by the fundi — not verified", with the date) and, on tap, an opted-in phone. It still hides email, Assessment videos, AI output and private items. Amends spec §7 and PRD US-5.7. | operator OD-1, OD-2 |
| D-21 | The Portfolio publish consent ships in English **and** Kiswahili, like the verification consent. Extends the AGENTS rule 5 exception; the operator applies the proposed AGENTS.md diff (find-a-fundi spec §14.1). | operator Q-2 |
| D-22 | Portfolio faces rule: the Fundi's own face is allowed; other people only with a per-item confirmation that they agreed; no recognisable children; no automated face detection. The DESIGN.md "no identifiable faces" rule still governs Smart Fundis' own imagery. | Architect, find-a-fundi spec §8.2 |
| D-23 | The styled `/fundis` and `/f/[id]`, and the Client-facing `/trades`, move from V3 to **V6 "Find a Fundi"**. V1 keeps its unstyled `/f/[id]`. Demo profiles have no phone, no Portfolio and no Expert mark. Timing amended by D-27. | operator OD-4, `planning/slices/V6.md` |
| D-24 | **Every Listed Fundi appears on `/fundis`** (a Fundi profile, `publicListing` on, not hidden by an Admin). A Badge is not required: verification is shown, not required. Unverified Fundis carry a neutral "Not yet verified" label (dim, never amber, never implying rejection). Verified Fundis sort first, and a "Verified only" chip filters. The Trade filter matches declared Trades too ("Not yet verified in <Trade>"), and Fundis may declare any of the 12 Trades. An AI `pass` awaiting review still shows nothing public. Amends D-2 and spec §5; supersedes D-19. | operator Q-1, ADR-22 |
| D-25 | **Expert verifier mark:** a Fundi who is an active Expert (`experts.active` and a non-empty `approvedTrades`) shows "Expert verifier · <Trade>" on the card and profile. It is distinct from skill Badges (no ✓, not amber), derived at sync time and never stored on the profile, and never says "certified". It counts as verified for sorting and the chip in that Trade only. Experts without a Fundi profile are not listed. | operator Q-1, ADR-22 |
| D-26 | LinkedIn, CV and personal-portfolio links are public only through a **per-link opt-in** (`fundiProfiles.linksPublic`, off by default). Turning on the CV link warns "CVs often include your home address. Remove it first." YouTube and TikTok stay "Showcase — not verified". | operator Q-4 |
| D-27 | The V6 **discovery core** (V6-1 projection and search, V6-2 `/fundis`, V6-3 `/trades`, V6-8 Demo Listings) runs **right after V1, before the V5 demo**, and V5's `seed.demo` calls `syncListing`. The rest of V6 follows. Amends D-6 (slice order) and D-23. | operator Q-7 |
| D-28 | Real `/f/[id]` and `/fundis` pages may be indexed by search engines, and Demo profiles are `noindex` (Q-5). A Fundi disputes an Admin hide by emailing **info@smartfundis.com**, with no in-app appeal in V6 (Q-6). No Turnstile in V6 (Q-3, R-18). | operator |
| D-29 | **Superseded by D-64 (English only for now).** **Kiswahili is agreed.** From V2 the UI ships in **English and Kiswahili**: every user-visible string lives in both `messages/en.json` and `messages/sw.json`, the language toggle is shown (PRD US-1.4), and the Fundi sees `feedbackSw` under the same rules as `feedbackEn` (an AI suggestion, never a decision). Supersedes the "English only" amendment to ADR-17 and AGENTS rule 5. Kiswahili copy and AI output get a native-speaker check, because no Nemotron model card lists Kiswahili (R-20). | operator, 2026-09-26 |
| D-30 | **Clients have accounts in V2.** Every role signs in: Fundi, Client, Expert and Admin. Clients get a dashboard and post Jobs (the V2 plan in `planning/prompts/v2/`). Supersedes D-2's "no Client account" for V2, and moves "Client accounts" off `/roadmap` once the feature is live. The V2 spec (V2-20) sets how the Client role is derived (ADR-18) and which Client actions need sign-in. | operator, 2026-09-26 |
| D-31 | **Demo Clients.** The demo seed creates made-up Client accounts with mock data (for example Jobs), like Demo profiles: invented names, no real person's details, and a visible **"Demo"** tag. | operator, 2026-09-26 |
| D-32 | **Nemotron is self-hosted with vLLM on Brev GPUs**, using `nemotron-3-nano-30b-a3b` or `nemotron-3-super-120b-a12b` through a LangChain chat model. Supersedes ADR-4. Research note: Super needs at least 8× H100-80GB, so Nano fits the one-H100 box; see `docs/research/2026-09-26-nvidia-fit.md` §2.0a. | operator, 2026-09-26 |
| D-33 | **Proposed (V2 spec).** **Client is a derived role:** a User is a Client while a `clientProfiles` row exists. `Roles` becomes four booleans `{ fundi, client, expert, admin }`. `/dashboard` goes to `users.dashboardPref` if still held, else Admin → Expert → Fundi → Client. One User may be Fundi and Client; a Fundi can't respond to their own Job. Amends ADR-18; completes D-30. | operator V2-Q7; ADR-23; `docs/superpowers/specs/2026-09-26-v2-marketplace-design.md` §5 |
| D-34 | **Proposed (V2 spec).** **Jobs and Interests.** Clients post Jobs (`draft → open ⇄ shortlisted → hired → done`, plus `expired`, `cancelled`, `removed`). Any Fundi sends "I'm interested" + a note ≤ 280, no price. No in-app chat (a later, subscriber-only phase). **A Client's phone number and WhatsApp are never shown to a Fundi.** Contact runs Client → Fundi only, through logged mutations: any signed-in Client can reveal an opted-in Fundi's phone (ADR-21) or open a Fundi Pro's WhatsApp link (operator round 4); Pay-to details go to the hiring Client. Public `/jobs`, landing strip and JOBS tab show Trade, title, county · ward, budget, age and a response bucket only. Demo Jobs accept no Interests. Extends ADR-21. | operator V2-Q4–Q6, rounds 2–3; ADR-24; marketplace spec §6–§7, §12 |
| D-35 | **Proposed (V2 spec).** Job cards show a real, **bucketed response count** ("No responses yet", "1", "2–5", "More than 5"). No other counters. Amends find-a-fundi §7 rule 7 for Jobs only. | V2-Q14; marketplace spec §8.4 |
| D-36 | **Proposed (V2 spec).** **Location:** device `navigator.geolocation` snapped once to 0.01° (~1.1 km), a county → sub-county → ward picker (`areas`), a Job landmark shown only to a connected Fundi, **distance bands only**, no map, **no Google** (a `GeoProvider` interface with a `none` implementation). Supersedes PRD ADR-6; amends D-18. **Amended (V2-25 review, D-54/D-55):** no `GeoProvider` interface until a real provider exists; public Job bands come from the ward centroid. | operator V2-Q8; ADR-25; marketplace spec §7 |
| D-37 | **Proposed (V2 spec).** Proximity uses our own **geohash cells** behind a `ProximityIndex` interface, haversine in the query, ≤ 12 cells × 50 rows, near-me results capped at 60 and not paginated. `@convex-dev/geospatial` (beta) is the swap-in. `listings` gains private `geohash`/`point`, never returned (amends ADR-22 consequences). A `convex-expert` check is owed at V2-25. **Amended by D-54:** proximity uses `@convex-dev/geospatial` `nearest()` behind `ProximityIndex`; the own-geohash ring search is only the fallback. | marketplace spec §7.3 |
| D-38 | **Proposed (V2 spec).** **Price is a filter, never a rank**, for Rates, Budgets and any future Interest price. Paid placement is governed separately by D-44. Extends CONTEXT "Rates". | ADR-26; marketplace spec §8 |
| D-39 | **Proposed (V2 spec).** **Nemotron pre-fills a Job post** (Trade suggestions, title, county, area text, a budget only if typed) through a new pull pair `/ai/claim-parse` + `/ai/callback-parse` (ADR-9 holds); a keyword map runs first; the Client confirms every field; the 15 s wait falls back to the manual form. The Assessment contract is unchanged. **Amended by D-58:** V10 is last and optional, after V11, and cut unless Brev already runs for video. | marketplace spec §11.2; NF §2.1 |
| D-40 | **Proposed (V2 spec).** Every new account declares **18+** (`users.adultConfirmedAt`); Job photos need a per-photo "no children, everyone recognisable agreed" tick (extends D-22). **Extended (V2-25 review, rai S9):** existing Fundis and Experts confirm 18+ at their next sign-in, and uploads are blocked until they do. | research I-12, I-17; marketplace spec §12 |
| D-41 | **Proposed (V2 specs).** **Direct pay:** Clients pay Fundis directly to the Fundi's self-declared **Pochi la Biashara or Till** (Pochi is a phone number: revealed only to the hiring Client; Till optionally public). A Till or Pochi number is required only for Fundis who respond to Jobs, not to register (V2-Q47); verification-only Fundis are prompted, never blocked. Only the Client who hired the Fundi sees it. Smart Fundis' STK Push is only for Fundi Pro (V2-Q46, operator-confirmed by default). Smart Fundis never holds money; no escrow. **M-Pesa STK Push only for Smart Fundis' own fee: the Fundi Pro subscription**, paid to Smart Fundis' own Safaricom Till (operator rounds 2–3), sandbox with "Test mode" until the Till exists. `/roadmap` item "bookings & M-Pesa" becomes "Escrow & in-app payment to fundis". | operator V2-Q2, rounds 2–3; ADR-27; `docs/superpowers/specs/2026-09-26-v2-payments-design.md` |
| D-42 | **Proposed (V2 specs).** A new **`mpesa`** agent (`.claude/agents/mpesa.md`, draft) owns `convex/payments/`, the STK callback HTTP action, payment tables and contracts; **no UI** (it gives UI suggestions; frontend alone writes `web/`). A project skill `.claude/skills/mpesa-daraja/` is to be written from the Daraja portal. | operator V2-Q9; payments spec §11 |
| D-43 | **Proposed (V2 spec).** **Retention:** drafts 30 days; Job photos, points and landmarks 30 days after closing; Jobs, Interests and notices 12 months after closing; `contactShares` 90 days; `aiParseRequests` 24 hours; payer MSISDN 90 days after a payment closes. **Clarified by D-53:** "after closing" counts from the close, including a hired Job's 30-day auto-close to `done`. | V2-Q21, V2-Q28; marketplace spec §12.3 |
| D-44 | **Proposed (V2 specs; operator round 3).** **Fundi Pro and ranking.** Fundi Pro (a Fundi subscription paid to Smart Fundis) ranks **first, verified or not**: Pro verified → Pro not yet verified → verified → Demo → not yet verified, in Job responses **and** on `/fundis`. Every Pro result carries the "Fundi Pro" tag and every such list the legend "Pro = paid subscription, not a verification" (EN + SW); ✓/amber and "Verified by Smart Fundis" stay Badge-only; the "Verified only" chip still filters to verified. `listings` gains `subscriber`; subscription changes call `syncListing`. Fundi Pro is monthly, renewed by a manual STK Push, with an in-app reminder 10 days before it lapses; price KSh 200 a month (operator round 6). Only Fundi Pro is live in V2; **Enterprise Pro** (Clients with large or many Jobs, project management), subscriber in-app chat and AI voice search go on `/roadmap`. **Supersedes D-24's "verified Fundis sort first" and ADR-22's sortKey tiers (once approved).** **Amended by D-47** (the tag reads "FUNDI PRO · PAID" / "FUNDI PRO · AMELIPIA" on every result and on `/f/[id]`) **and D-48** (verified first on Electrical and Solar). | operator rounds 2–3; ADR-28; R-17, R-37 |
| D-45 | **Operator-decided (V2-25 review, 2026-09-26).** **Prerequisites.** V7 builds on the MVP **V1 stub** (stub worker and canned callbacks), because the dashboards only display stored fields. V8 stays blocked on the **V6 core** (Listings). The "this sprint" labels in the marketplace spec §21 give way to explicit prerequisites. V7 restyles the MVP Expert review; it does not build a second one. | `docs/reviews/v2-architecture.md` §1 Q1 |
| D-46 | **Operator-decided (V2-25 review, 2026-09-26).** **Contact.** Every reveal (Call, WhatsApp, Pay-to) needs `requireClient`: a Client profile with a phone, 18+ and the terms accepted. One `contact.reveal({ fundiProfileId, channel, jobId?, interestId? })` replaces `jobs.revealFundiPhone` and `jobs.openWhatsApp`; all reveal paths use one internal helper keyed on `userId`. V6's anonymous `visitorKey` reveal is **retired** (V2-Q24 yes). A per-Fundi cap of 100 a day counts across Call, WhatsApp and Pay-to. `clients.create` is rate-limited per identity. `contactShares.jobId` becomes optional and `contactReveals` is retired; V8 owns the migration. Amends ADR-21 and D-34. | `docs/reviews/v2-architecture.md` §1 Q2 |
| D-47 | **Operator-decided (V2-25 review, 2026-09-26).** **Pro disclosure on the result.** The tag reads **"FUNDI PRO · PAID"** (EN) and **"FUNDI PRO · AMELIPIA"** (SW), is a 48 px button that opens the US-8.32 explanation, and also appears on `/f/[id]`. The forbidden-word copy test allow-lists "Paid". The SW legend drops "usajili"; draft "Pro = amelipia ili aonekane kwanza, si uthibitisho wa ujuzi", pending a native-speaker check (R-20). Amends D-44. | `docs/reviews/v2-architecture.md` §1 Q3 |
| D-48 | **Operator-decided (V2-25 review, 2026-09-26).** **Safety Trades.** On **Electrical and Solar**, verified comes first and Pro is tagged within each tier: verified Pro → verified → Demo → not-yet-verified Pro → not yet verified. Every other Trade keeps Pro-first (D-44). Amends D-44. | `docs/reviews/v2-architecture.md` §1 Q4 |
| D-49 | **Operator-decided (V2-25 review, 2026-09-26).** **Kenya DPA transfers.** The verification consent and the Client terms each carry an explicit line (EN + SW) that data is processed outside Kenya: Convex and Clerk (US), Brev and LangSmith. Processor agreements for all four are kept on file. The **DPIA update gates V8's production deploy**, and V9's for location. | `docs/reviews/v2-architecture.md` §1 Q5 |
| D-50 | **Operator-decided (V2-25 review, 2026-09-26).** **Neutral wait.** Before the Expert decides, the Fundi sees one line for every Verdict: "Awaiting expert review". Nothing hints at the Verdict, the cap or a safety flag until the decision. | `docs/reviews/v2-architecture.md` §1 Q6 |
| D-51 | **Operator-decided (V2-25 review, 2026-09-26).** **Expert sees the Verdict late.** The AI Verdict is shown only after the video has played through once. The queue shows only the safety marker, never an "AI suggestion" chip. Expert–AI agreement rate and time to decide are logged and shown on the Admin eval tiles. | `docs/reviews/v2-architecture.md` §1 Q7 |
| D-52 | **Operator-decided (V2-25 review, 2026-09-26).** **"The AI noticed…"** is shown only on a reshoot or reject decision, never on an approved Assessment, in V2 (V2-Q20 narrowed). **Superseded by D-59.** | `docs/reviews/v2-architecture.md` §1 Q8 |
| D-53 | **Operator-decided (V2-25 review, 2026-09-26).** **Hired Jobs auto-close** to `done` 30 days after `hiredAt`, with a notice to the Client. Retention counts from the close (clarifies D-43). | `docs/reviews/v2-architecture.md` §1 Q9 |
| D-54 | **Operator-decided (V2-25 review, 2026-09-26).** **Proximity** uses `@convex-dev/geospatial` `nearest()` behind the `ProximityIndex` interface, one call per tier (`filterKeys: { scope, tier }`), storing only snapped points. The custom precision-5 ring search stays as the fallback behind the same interface. `convex-expert` confirms in V9's first ticket. Supersedes D-37's own-geohash choice and the "no `@convex-dev/geospatial`" line in the marketplace spec §22. | `docs/reviews/v2-architecture.md` §1 Q10 |
| D-55 | **Operator-decided (V2-25 review, 2026-09-26).** **Job location precision.** Public and pre-hire Job bands come from the **ward centroid**, never the Client's point; `clientProfiles` stores **no point**. Results sort **by band**, never by exact km. The server snaps every query point before computing bands. Copy says "others can tell only roughly which area (about 1 km)", not "never shown". `fundi.setServiceArea` is rate-limited. The ~1 km cell is the accepted floor. | `docs/reviews/v2-architecture.md` §1 Q11 |
| D-56 | **Operator-decided (V2-25 review, 2026-09-26).** **V8 imports ward names only** (IEBC / LN 14/2012) for the six first counties, so V2-Q11 blocks V8. Point capture and centroids stay in V9. | `docs/reviews/v2-architecture.md` §1 Q12 |
| D-57 | **Operator-decided (V2-25 review, 2026-09-26).** **Navigation.** Public bar: **JOBS · TRADES · EVIDENCE · COMPANY**, with TELEMETRY moved into the COMPANY sheet and the footer. Role routes (`/client`, `/fundi`, `/expert`, `/admin`) use a role bar of at most 4 items. Role switcher in the avatar sheet, bell in the header, EN \| SW toggle in the menu sheet, the footer and the desktop header. | `docs/reviews/v2-architecture.md` §1 Q13 |
| D-58 | **Operator-decided (V2-25 review, 2026-09-26).** **Nemotron Job pre-fill (V10) is last and optional:** after V11, and cut unless Brev already runs for video. It keeps every reviewer fix (review §3.4). gpu-devops measures p95 parse latency while Cosmos is busy before V10 is cut. Amends D-39. | `docs/reviews/v2-architecture.md` §1 Q14 |
| D-59 | **Operator-decided (V2-40 design, 2026-09-26).** **The Fundi sees the AI evidence after the Expert decides.** On every decision (approved, reshoot, rejected), the Fundi's Assessment detail shows "The AI noticed…" Observations with their evidence text and whole-second timestamps, under the AI tag and "An Expert decides.", below the Expert's note. D-50 is unchanged: before the decision, nothing but "Awaiting expert review". Still never a Verdict word, `confidence`, `videoUrl`, `livenessRead` digits or a model name; safety items read "needs a closer look". Timestamps show as plain text to the Fundi until the Architect decides whether the Fundi gets a player of their own video. **Supersedes D-52.** Spec §11.0–§11.1, US-8.27 and test 40 need updating to match. | V2-40 Step 1 (DESIGN.md D5, Q5) |
| D-60 | **Operator-decided (V2-40 design, 2026-09-26).** **No map in V2.** The spec wins over the V2-40 brief: location is county/area text plus a distance band on every screen (D-36, R-21, spec §7.2 and §7.6). "Jobs near me" and "find a Fundi" are lists only. DESIGN.md D6 part B is kept only as a post-V9 option. Expert review keeps the spec's wording "AI suggestion — you decide"; every other AI block says "An Expert decides." | V2-40 Step 1 (DESIGN.md Q1–Q3) |
| D-61 | **Operator-decided (V2-40 design, 2026-09-26).** **D-59 guardrails.** (1) The Cosmos `evidence` text stays English in both UI languages, labelled "AI note in English" (SW draft pending R-20); the Rubric item, the AI's answer and all chrome are translated. This is a named exception to D-29. (2) The approved-state "The AI noticed…" copy goes to `rai-reviewer` before V7-10 ships (framed as practice notes, never as doubt about the Badge). (3) The Fundi sees **no timestamps** until the timestamp-reliability eval passes (NF §1.1); after that, plain text. (4) The Fundi-facing query redacts the liveness code (and any digit run matching it) from `evidence`, and test 40 plants the code in `evidence` to prove it. Amends D-59. | V2-40 Step 1 (architect conflicts on D-59) |
| D-64 | **Operator-decided (2026-09-26, during #37).** **English only for now.** The UI is built in English only, **consent screens included**. Kiswahili is a future phase. Strings still go through next-intl (`en.json` only). No new SW drafts and no language toggle. Existing SW draft files are kept. Supersedes D-29's EN + SW rule and the "consent stays bilingual" rule (PRD §8). D-62 and D-63 are taken on the `docs/v2-40-screens` branch. | operator, #37; AGENTS.md rule 5; R-44 |

**Proposed amendments pending approval of the V2 specs:** D-18 ("Geohash, maps and distance stay out (ADR-6)") would be amended by D-36/D-37; PRD ADR-6 would be superseded by ADR-25; D-24's "Verified Fundis sort first" and ADR-22's sortKey tiers would be superseded by D-44/ADR-28. They are not struck through until the operator approves the specs.

D-45 to D-58 are the operator's answers to the V2 architecture review (grilling round 1, every recommendation accepted); the full findings and mechanical fixes are in `docs/reviews/v2-architecture.md`.

D-17 is taken by the frontend proposal on the `v3/landing-page` branch; these rows start at D-18 to avoid a clash when it merges.
