# V3 — Trust and front door

- **Spec issue:** not published yet
- **Lane:** A (frontend + auth + convex) · **Blocked by:** V1 · **Time box:** to H+4. The Stitch work is capped at 45 minutes, with a checkpoint after the header, footer and landing.

## Demo at the end
- **At 360 px:** the Stitch-designed landing page (hero, How it works with the paper code, the 12 trades, Trust with "Become a verifier", "Coming next: earn from your skills", the footer), plus `/roadmap` and `/fundis`.
- **Joining:** "Join as a fundi" from the Electrical tile opens onboarding with Fundi and Electrical pre-ticked. The full Fundi profile form follows.
- **Before any upload:** the consent screen from V1 appears, now in the Stitch style.
- **On the Fundi dashboard:**
  - guard reshoot reasons
  - the Co-op card with its toggle
  - the "Show my profile in Find a fundi" toggle
- **On `/fundis`:** only Verified Fundis who have a public listing appear.

## In scope
- The Stitch run from `design/stitch/prompts/`, with the exports converted to shadcn components, tokens and `en.json` keys.
- Screens: the header and footer, landing, `/roadmap`, `/fundis` (with a trade filter), and the restyled V1 screens.
- Onboarding with two checkboxes, the `?as=` and `&trade=` parameters, the full Fundi profile (US-2.3), and Showcase embeds.
- Restyling the V1 consent screen, plus `coopInterest` and `publicListing`.
- Lighthouse mobile ≥ 90 for performance and accessibility.

## Grill focus
1. How the consent screen is restyled. Its wording was settled in V1.
2. Pass or fail rules for the Stitch checkpoint, and what happens if the 45 minutes run out.
3. The empty state of `/fundis` before any Badge exists, alongside the Demo profiles.
4. How Showcase embeds load (lazy, a tap to load) against the Lighthouse target.

## Risky tickets that need a plan first
None. The Stitch run is `ready-for-human` plus agent work.

## Human steps
The Stitch checkpoint, the Kiswahili consent review, the Lighthouse run on a real phone.
